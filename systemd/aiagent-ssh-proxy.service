[Unit]
Description=aiagent-ssh-proxy - SSH reverse proxy for AI agents
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=root
Group=root
WorkingDirectory=/data/aiagent-ssh-proxy
Environment=SSH_LISTEN_ADDR=
Environment=WEB_LISTEN_ADDR=127.0.0.1:8080
EnvironmentFile=-/data/aiagent-ssh-proxy/aiagent-ssh-proxy.env
ExecStart=/data/aiagent-ssh-proxy/aiagent-ssh-proxy \
  -db /data/aiagent-ssh-proxy/aiagent-ssh-proxy.db \
  -host-key /data/aiagent-ssh-proxy/host_key

Restart=on-failure
RestartSec=2s

# 服务按部署约定使用 root 运行,但仍通过 systemd 限制文件系统和提权能力。
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/data/aiagent-ssh-proxy
PrivateTmp=true
UMask=0077

[Install]
WantedBy=multi-user.target
